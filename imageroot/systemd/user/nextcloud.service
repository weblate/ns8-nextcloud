#
# This systemd unit starts a nextcloud instance using Podman.
# Most parts of this file come from podman-generate-systemd.
#
[Unit]
Description=nextcloud server

[Service]
Environment=PODMAN_SYSTEMD_UNIT=%n
EnvironmentFile=%S/state/environment
WorkingDirectory=%S/state
Restart=always
ExecStartPre=/bin/rm -f %t/nextcloud.pid %t/nextcloud.ctr-id
ExecStart=/usr/bin/podman run \
    --detach \
    --conmon-pidfile=%t/nextcloud.pid \
    --cidfile=%t/nextcloud.ctr-id \
    --cgroups=no-conmon \
    --replace --name=%N \
    --publish=127.0.0.1:${TCP_PORT}:8080 \
    ${ECHO_SERVER_IMAGE}
ExecStop=/usr/bin/podman stop --ignore --cidfile %t/nextcloud.ctr-id -t 10
ExecStopPost=/usr/bin/podman rm --ignore -f --cidfile %t/nextcloud.ctr-id
PIDFile=%t/nextcloud.pid
Type=forking

[Install]
WantedBy=default.target
